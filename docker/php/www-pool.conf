[www]
; Run worker processes as the "www" user so created files match the
; bind-mounted source ownership. The master still runs as root (required).
user = www
group = www

; Listen on the TCP socket nginx connects to.
listen = 9000

; Make sure PHP errors surface in `docker compose logs app` during development.
catch_workers_output = yes
decorate_workers_output = no
